type Customer @model {
  id: ID!
  customerEmail: String!
  customerSurvey: Survey @connection(name: "CustomerSurvey")
}

type Survey @model {
  id: ID!
  surveyCustomer: Customer @connection(name: "CustomerSurvey")
}